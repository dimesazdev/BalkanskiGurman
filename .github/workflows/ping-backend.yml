name: Ping Render Backend

on:
  schedule:
    - cron: '*/5 * * * *'  # Every 5 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Wake up backend
        run: curl -m 10 https://balkanskigurman.onrender.com/restaurants
